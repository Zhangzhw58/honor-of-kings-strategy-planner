# AGENTS.md – 王者荣耀局势规划器

本文件适用于仓库根目录及其所有子目录，用于为 AI 编程助手提供项目上下文、开发约束与协作指南。开始工作前应完整阅读本文件。

## 项目概览

- 项目名称：王者荣耀局势规划器（Honor of Kings Situation Planner）。
- 当前版本：0.2，处于 MVP 需求与架构设计阶段。
- 当前仓库尚未落地具体语言、框架、构建系统或测试工具；技术建议不等于已安装依赖。
- 产品目标：从 Android 对局截图、录屏流或视频片段中抽取局势状态，并在 2～5 秒内给出简短、可执行的战术建议。
- 第一版重点：打野视角、规则驱动决策、结构化状态、敌方关键英雄位置预测、手机端短文本提示。
- 权威需求文档位于 `docx/01-项目需求说明.md`。实现产品功能前应先阅读该文档；若代码、其他文档与它冲突，应指出冲突，不要擅自改变产品目标。

## 核心产品约束

- 产品是决策辅助工具，不得实现自动操作、输入注入、规避检测或其他外挂能力。
- 建议必须包含明确的“动作 + 位置”，并尽可能补充理由、置信度或风险提示。
- 面向玩家的实时建议保持 1～2 句，原则上不超过约 30 个汉字。
- 优先保证建议有意义、可解释和及时；不要为了模型复杂度牺牲 2～5 秒的端到端延迟目标。
- 第一版采用“规则引擎 + 经验模型”的分层方案，不要未经讨论直接替换为不可解释的纯端到端决策模型。
- 保持输入采集、状态识别、局势重建、决策、建议生成和反馈模块之间的边界，避免把识别逻辑、策略规则与 UI 展示耦合在一起。
- Android 优先，但领域模型和核心决策接口应尽量与具体客户端解耦，为后续 Web、iOS、回放分析和服务化保留空间。
- 实时推理优先考虑 On-Device 方案，避免不必要的云端网络延迟；引入云端链路前必须评估隐私、离线可用性和延迟预算。
- 默认抽帧频率不高于 2 Hz。提高频率前应通过数据证明收益，并评估 CPU、内存、温度和功耗。

## 模块架构与数据流

系统按以下职责保持严格解耦：

| 模块 | 职责 |
| --- | --- |
| 输入采集 | 通过 Android 截图或录屏获取画面流，负责抽帧、缓存和生命周期管理。 |
| 状态识别 | 检测英雄、兵线、防御塔、野怪、资源、小地图等画面元素。 |
| 局势重建 | 将识别结果聚合为结构化状态，表达时间、位置、生命值、技能和资源差。 |
| 决策器 | 根据局势状态计算优先级，输出动作、目标位置、理由、置信度和风险。 |
| 建议生成 | 将结构化决策转换成短文本，并封装为通知、悬浮窗或语音输出。 |
| 反馈与标注 | 收集反馈和标注修正，为规则与模型迭代提供数据。 |

- 主数据流：画面 → 状态识别 → 局势重建 → 决策 → 建议生成 → 展示。
- 反馈流：用户反馈 → 标注修正 → 规则或模型更新。
- 模块间通过版本化的结构化契约通信；不得让 UI 直接依赖识别模型的内部输出。

## 候选技术方向

以下内容是尚待实现验证的优先方向，不是强制依赖。建立对应模块时，应结合性能原型、包体、许可证和维护成本做最终选择并记录设计决策。

- Android 端优先使用 Kotlin；通过 `MediaProjection` 等平台能力采集屏幕，通过 Coroutines 处理异步任务，通过 `StateFlow` 管理可观察状态。
- 离线训练和数据处理优先使用 Python；实验需固定随机种子，并保存可复现的配置、数据版本和评估结果。
- 图像处理可评估 OpenCV；OCR 可评估 Tesseract；目标检测可评估 MediaPipe 或适合端侧部署的 YOLO 变体。
- 第一版规则保持轻量、确定且可追溯。仅当规则规模确实需要时再评估 Easy Rules、Drools 等框架，避免过早引入重量级运行时。
- Android 本地状态持久化可评估 Room；仅在确有跨进程需求时使用 AIDL 或 Messenger。
- 悬浮提示可评估 Android `WindowManager`，并正确处理授权、生命周期、前后台切换及不同系统版本限制。
- 训练或数据模块建立后，Python 默认评估 `black` 和 `pylint`；Android 模块遵循 Kotlin 官方编码规范。以仓库实际配置为最终执行标准。

## 工作方式

- 修改前先检查相关文档、现有实现、配置和测试，不要根据项目名称猜测技术栈。
- 只做任务要求内的最小必要改动；不要顺手重构无关代码或覆盖用户已有改动。
- 若仓库仍缺少实现所必需的技术选型，先记录假设与权衡；会显著影响架构、依赖或交付形式时，应请求用户确认。
- 优先修复根因，避免仅隐藏错误、吞掉异常或写死只对单一样例有效的结果。
- 新增第三方依赖前说明用途，优先使用已有依赖和平台能力；不要提交密钥、令牌、账号、私有视频或受版权限制的数据。
- 涉及游戏素材、录屏和标注数据时，使用脱敏的小型样例或生成数据，并说明其来源与许可条件。
- 不修改、移动或删除与当前任务无关的文件。工作树不干净时，保留并绕开既有改动。
- 不假设存在 `dev` 分支或固定 Git 流程。当前仅确认 `main` 分支；如需建立分支策略，应先形成团队约定。
- 提交信息优先使用 Conventional Commits，例如 `feat: add enemy position predictor`；除非用户明确要求，不要自行创建分支或提交。

## 代码与接口约定

- 遵循所选语言的官方风格以及仓库中已经建立的格式化、静态检查和命名规则。
- 名称应体现领域语义，例如 `GameState`、`TacticalAction`、`MapRegion`、`RiskLevel`；避免含糊缩写和无意义的单字母变量。
- 对关键算法、非显然的规则判断和状态字段补充简洁注释，重点解释设计意图和约束，不要复述代码行为。
- 核心状态与建议使用结构化类型表达，不要依赖自由文本在模块之间传递业务状态。
- 时间、坐标、置信度、阵营和地图区域必须明确单位或枚举语义；不要在模块间传递含义不明的裸数字。
- 规则决策应保持确定性和可测试性。同一状态输入应得到稳定结果，并能追溯触发规则和理由。
- 识别或预测结果必须允许表达未知、失去视野和低置信度，不得把推测伪装成确定事实。
- 实时链路避免阻塞主线程；耗时识别、推理和 I/O 应具备超时、取消、降级与错误传播策略。
- 用户可见文本默认使用简体中文；代码标识符和机器接口字段默认使用英文，除非既有代码另有约定。

## 数据与标注约定

- 数据来源优先级：合法的自行录制数据、许可明确的公共比赛素材、用于补充极端场景的合成数据。
- 每个帧或短片段样本至少应表达 `timestamp`、`phase`、`state`、`action`、`next_action`、`target_location` 和 `enemy_positions`。
- `phase`、动作和区域等有限集合应使用受控枚举或带版本的标签表，避免同义词造成标签漂移。
- `enemy_positions` 应表达候选区域、概率或置信度、最后可见时间；位置概率应满足明确的归一化与未知状态约定。
- 标注数据优先采用 JSON；需要扁平批处理时可使用 CSV。无论格式如何，都应提供 schema、版本和最小合法样例。
- 标注工具可评估 Label Studio 或自定义 Web 工具，优先保证局势判断质量和标注一致性，而不是单纯追求数量。

## 决策器原则

- 采用双层架构：第一版以可解释的专家规则为主，后续可使用决策树、随机森林或经验证的学习方案增强复杂场景判断。
- 基线优先级为资源争夺 > 推塔 > 打团 > 视野控制，但安全约束、目标可达性、存活状态和高置信度风险可以覆盖该排序。
- 高价值资源可争夺且视野安全时优先抢占；敌方打野失去视野时，降低激进行动置信度并优先规避或补充信息。
- 决策器必须输出结构化对象，至少包含 `action`、`target`、`reason`、`confidence` 和 `risk`；展示文案不是决策模块的唯一输出。
- `confidence` 的范围、校准方法和阈值必须文档化；`risk` 使用受控等级，并允许表达未知。

## 测试与验收

- 每次功能修改都应补充或更新与行为最接近的测试；优先覆盖正常路径、边界条件、未知状态和低置信度场景。
- 决策规则测试至少验证输入状态、推荐动作、目标区域、理由及风险/置信度，而不只比较最终文案。
- 局势重建与决策模块必须有自动化单元测试；Android 模块优先使用 JUnit，Python 模块优先使用 PyTest，最终以实际工具链为准。
- 画面识别测试优先使用尺寸受控、可合法提交的固定样例；不要把完整录屏或大型模型权重直接加入 Git。
- 涉及实时链路时，记录并验证采集、识别、决策和展示的耗时，关注 2～5 秒总延迟目标。
- 当前仓库没有可用的构建、格式化或测试命令。不要声称已运行不存在的命令；待工具链建立后，在本节补充准确命令。
- 交付前至少检查 `git diff --check` 和 `git status --short`，并在总结中列出实际运行的验证及未验证项。

## 文档维护

- 产品需求或范围变化应同步更新 `docx/01-项目需求说明.md`，并更新文档中的版本号和日期。
- 引入技术栈后，应补充根目录 `README.md`，说明环境要求、项目结构、启动方式、测试命令和配置方法。
- 新增环境变量时提供不含秘密值的示例文件，并说明是否必填、默认值和用途。
- 重要架构选择、数据格式或模型选择应形成可追溯的设计记录，不要只留在代码注释或提交信息中。

## 提交前检查

- 确认实现符合需求文档中的产品边界和 MVP 优先级。
- 确认没有提交敏感信息、大型二进制、原始用户数据或来源不明的游戏素材。
- 确认错误与低置信度场景有明确行为，不会输出误导性的确定结论。
- 确认文档、测试和实际行为保持一致。
- 最终说明应简洁列出修改内容、验证结果、已知限制和必要的后续步骤。
